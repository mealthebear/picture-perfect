const Client = require('./model.js');

// Get a client
const getAClient = () => {
    
}

// Get all clients
const getAllClients = () => {
    return Client.findAll();
}

// Add a client

// Update a client

// Delete a client

module.exports = {
    getAllClients
}